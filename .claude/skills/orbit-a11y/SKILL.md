---
name: orbit-a11y
description: Accessibility and design-system quality contracts for Orbit, Efficio's design system. Use whenever building or reviewing Orbit or @efficio/orbit UI for accessibility — accessible names for inputs (Input, Textbox, TextArea, CurrencyInput, DateInput, Searchbox, Dropdown), native checkbox/switch semantics, dialog naming and focus restoration for overlays, non-interactive cards, labelled vs decorative spinners, no raw hex in reusable components, token-owned sizing, and WCAG 2.2 AA contrast (4.5:1 text, 3:1 large/UI). These are enforced by the static audit — run `npm run audit:design-system` to verify. For tokens use orbit-tokens; for component APIs use orbit-components; for principles use orbit-foundations.
---
<!-- GENERATED by scripts/build-skills.ts — do not edit by hand. Edit the token source or design-md/prose/skills/, then run `npm run build:skills`. -->

# Orbit accessibility & quality

Orbit ships accessibility as **enforced contracts**, not aspirations. 27
of them are checked by `scripts/design-system-static-audit.mjs`; run
`npm run audit:design-system` before sharing component changes.

- The full machine-enforced checklist → `references/audit-checks.md`
- The input accessible-name contract → `references/input-naming-contract.md`

## Contrast

Orbit targets **WCAG 2.2 AA**:

- **4.5:1** for body text against its background.
- **3:1** for large text (≥ 24px, or ≥ 18.66px bold) and for non-text UI (borders,
  icons, control boundaries, focus rings).

Never communicate state with colour alone. Error states pair the error colour with
an icon **and** accessible text; status chips carry a label, not just a hue.

## Keyboard & focus

- Every interactive element is reachable and operable by keyboard. Don't replace a
  native control with a styled `div`.
- Focus is always visible — the focus ring uses `--orbit-color-focus-ring`. Don't
  remove outlines without providing an equivalent visible indicator.
- Overlays and dialogs (`Overlay`) expose a dialog name, keep focus inside while
  open, and **restore focus** to the trigger on close.
- Containers stay non-interactive. `Card` is a container, not a button — put the
  action on a child button or link rather than a `role="button"` wrapper.

## Naming

Inputs require a deliberate accessible name (visible `label`, `ariaLabel`, or
`ariaLabelledBy`). See `references/input-naming-contract.md` for the per-component
contract; the dev-only `warnIfUnnamed` helper flags any field left unnamed.
